/**
 * Demonstrations of several implementations of the "singleton" pattern in which
 * objects possess their own "creational" behavior via implementing a static
 * "getInstance()" method.
 * 
 * @author mross
 */
package mdr.ap.patterns.singleton;